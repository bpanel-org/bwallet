 const {
  updateTextField,
 } = require('./interface');

const {
  getWallets,
  selectWallet,
  createWallet,
} = require('./wallet');

module.exports = {
  updateTextField,
  getWallets,
  selectWallet,
  createWallet,
};
